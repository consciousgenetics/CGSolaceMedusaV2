[build]
builder = "NIXPACKS"
buildCommand = "cd .medusa/server && yarn install && yarn predeploy"

[deploy]
startCommand = "cd .medusa/server && yarn run start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3 