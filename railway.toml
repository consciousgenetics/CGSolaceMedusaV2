[build]
builder = "NIXPACKS"
buildCommand = "yarn install && yarn migrate && yarn seed && yarn build"

[deploy]
startCommand = "yarn start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3 