import { SubscriberArgs, SubscriberConfig } from '@medusajs/medusa';
export default function orderPlacedHandler({ event, container, }: SubscriberArgs<any>): Promise<void>;
export declare const config: SubscriberConfig;
